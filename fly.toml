# QHUB Studio — Fly.io deployment config
# Fly.io is the fastest path to get QHUB Studio live with zero infra setup.
# AWS App Runner is the production target; use Fly for staging / early access.
#
# Deploy: fly deploy
# Secrets: fly secrets set QHUB_HMAC_SECRET=... SUPABASE_URL=... etc.

app = "qhub-studio"
primary_region = "iad"   # us-east (IAD) — closest to NY financial clients

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  VITE_LOG_LEVEL = "warn"
  PORT = "5173"
  # Non-secret build-time vars only.
  # Runtime secrets (QHUB_HMAC_SECRET, SUPABASE_URL, etc.) set via:
  #   fly secrets set KEY=value

[http_service]
  internal_port = 5173
  force_https = true
  # Keep the machine always-on: auto-stop caused ~40s cold-start 503s mid-session.
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = "requests"
    soft_limit = 200
    hard_limit = 250

[[vm]]
  size = "shared-cpu-2x"
  # 1gb was too tight for wrangler pages dev serving the heavy Studio SSR bundle
  # (instability / restarts under load). 2gb gives headroom.
  memory = "2gb"

[checks]
  [checks.alive]
    port = 5173
    type = "http"
    interval = "30s"
    timeout = "5s"
    grace_period = "45s"   # covers wrangler pages dev cold-start bundling
    method = "GET"
    path = "/login"        # / 302-redirects to /login once auth is enforced; /login is 200

